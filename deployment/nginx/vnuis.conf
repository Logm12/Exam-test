# Cấu hình Nginx cho fdbtalent.vnuis.edu.vn
server {
    listen 80;
    server_name fdbtalent.vnuis.edu.vn;

    # Giao diện chính (Frontend)
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Tăng timeout để tránh lỗi 504 cho frontend
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }

    # NextAuth (Handled by Next.js Frontend)
    location /api/auth {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
    }

    # API Backend (FastAPI)
    location /api/v1 {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Tối ưu hóa timeout cho API xử lý nặng (chấm điểm, export)
        proxy_read_timeout 300s;
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
    }

    # Docs/Docs Open API
    location /docs {
        proxy_pass http://127.0.0.1:8000;
        proxy_read_timeout 300s;
    }

    location /redoc {
        proxy_pass http://127.0.0.1:8000;
        proxy_read_timeout 300s;
    }

    # Uploads (Ảnh, đề thi - Served by Backend)
    location /uploads {
        proxy_pass http://127.0.0.1:8000;
        proxy_read_timeout 300s;
    }

    client_max_body_size 100M;
}
